<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"> <!-- Définir l'encodage des caractères en UTF-8 -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Assurer une bonne mise en page sur les appareils mobiles -->
    <title>Publications</title> <!-- Titre de la page -->
    <link rel="stylesheet" href="/css/styles.css"> <!-- Lien vers la feuille de styles CSS -->
</head>
<body>
    <h1>Publications</h1> <!-- En-tête principal de la page -->

    <!-- Formulaire de création de publication -->
    <form action="/posts" method="POST"> <!-- Le formulaire utilise la méthode POST et envoie les données à l'URL /posts -->
        <textarea name="text" placeholder="Écrire une publication..."></textarea> <!-- Champ de texte pour écrire la publication -->
        <button type="submit">Publier</button> <!-- Bouton pour soumettre le formulaire -->
    </form>

    <!-- Liste des publications -->
    <ul>
        <!-- Boucle pour chaque publication -->
        {{#each posts}}
            <li>
                <!-- Afficher le nom de l'utilisateur et la date formatée de la publication -->
                <p><strong>{{name}}</strong> - {{formatDate timestamp}}</p>
                <!-- Afficher le texte de la publication -->
                <p>{{text}}</p>
                <!-- Afficher le nombre de likes pour la publication -->
                <p>Likes: {{likes}}</p>
            </li>
        {{/each}}
    </ul>
</body>
</html>
